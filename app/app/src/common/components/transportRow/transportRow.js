
import transportRowComponent from './transportRow.component';

let transportRowModule = angular.module('transportRow',[])

.component('transportRow', transportRowComponent);

export default transportRowModule;